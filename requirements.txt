Django>=5.0.3,<5.1
psycopg2-binary>=2.9.7
python-dotenv>=1.0.0
django-crispy-forms>=2.0
crispy-bootstrap5>=0.7
social-auth-app-django>=5.2.0
argon2-cffi>=23.1.0  # For Argon2 password hashing
django-csp>=3.7  # For Content Security Policy
django-ratelimit>=4.1.0  # For rate limiting
django-axes>=6.1.1  # For login attempt tracking and blocking
django-widget-tweaks>=1.4.12
django-storages>=1.14.2  # For secure file storage
boto3>=1.28.0  # For AWS S3 and Cloudflare R2 support
django-cors-headers>=4.3.1  # For CORS protection
django-defender>=0.9.7  # For brute force protection
django-session-security>=2.6.7  # For session security
django-two-factor-auth>=1.15.5  # For two-factor authentication (optional)
django-otp>=1.3.0  # For one-time passwords (optional)
bleach>=6.1.0  # For sanitizing HTML input
django-honeypot>=1.0.1  # For spam protection
django-admin-honeypot>=1.1.0  # For admin login protection 
# Production WSGI server
gunicorn>=21.2.0
whitenoise>=6.6.0  # For serving static files in production
dj-database-url>=2.1.0  # For parsing DATABASE_URL on Render

# Data processing and analysis
pandas>=2.0.0
numpy>=1.24.0

# Excel file handling
openpyxl>=3.1.0
xlsxwriter>=3.1.0

# PDF generation
weasyprint>=60.0
reportlab>=4.0.0

# Image processing (if needed for media handling)
Pillow>=10.0.0 